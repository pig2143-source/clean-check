# 部署 DP Clean v6.2

1. 將專案根目錄內容覆蓋至 GitHub `main` 分支。
2. Commit 建議：`Remove AI photo analysis v6.2`。
3. 等待 Vercel 部署顯示 `Ready`。
4. 回到網站按 `Ctrl + F5`。
5. Vercel 中的 `OPENAI_API_KEY` 與 `OPENAI_VISION_MODEL` 可刪除。

本次不需要執行 Supabase SQL。
